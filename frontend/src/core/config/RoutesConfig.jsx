const RoutesConfig = {
	BASE: '/',
	REGISTRATION: '/registration',
	MAIN: '/main',
	BROWSE_COURSE: '/course/:courseId',
	LESSONS: '/lessons/:courseId',
};

export const { BASE, REGISTRATION, MAIN, BROWSE_COURSE, LESSONS } =
	RoutesConfig;
